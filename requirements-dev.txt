# See3D E57 Converter — build / development dependencies
# Installs everything in requirements.txt plus the .app packager.
# build.sh and CI install from this file.

-r requirements.txt

# ---- packaging ----
pyinstaller>=6.10
